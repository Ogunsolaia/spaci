% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recoverU.R
\name{recoverUplus}
\alias{recoverUplus}
\title{recoverU+: doubly robust ATT under spatial confounding and interference}
\usage{
recoverUplus(
  Y,
  Z,
  X,
  coords,
  tau = 0.1,
  normalize = TRUE,
  matern_method = c("mle", "geoR"),
  level = 0.95
)
}
\arguments{
\item{Y}{Numeric outcome vector.}

\item{Z}{Binary treatment vector (0/1).}

\item{X}{Covariate matrix or data frame.}

\item{coords}{Two-column matrix or data frame of coordinates.}

\item{tau}{Positive spatial decay parameter for the exposure kernel used in
the initial outcome model (default \code{0.1}).}

\item{normalize}{Logical; row-normalise the exposure kernel (default \code{TRUE}).}

\item{matern_method}{Matern estimation engine: \code{"mle"} (default, no external
dependency) or \code{"geoR"} (reproduces the reference analysis).}

\item{level}{Confidence level for the reported interval.}
}
\value{
An \code{idaps_fit} object.
}
\description{
The recoverU+ estimator augments the doubly robust ATT with both a partially
recovered spatial confounder and a neighbourhood-exposure term, so that the
propensity-score and control-outcome models adjust for spatial confounding
and spatial interference simultaneously (Equations 2.10-2.12). Compared with
\code{\link[=recoverU]{recoverU()}}, the neighbourhood exposure \code{G} is included in the PS and
control-outcome models.
}
\examples{
sim <- simulate_spatial_causal(n = 150, seed = 1)
recoverUplus(sim$Y, sim$Z, sim$X, sim$coords)
}
