% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_spatial_causal}
\alias{simulate_spatial_causal}
\title{Simulate spatial data with confounding and interference}
\usage{
simulate_spatial_causal(
  n = 250,
  true_att = 2,
  beta0 = 2.5,
  beta1 = 1,
  beta2 = 0.5,
  theta_spatial = 0.4,
  gamma_interference = 1.5,
  sigma_eps = 1,
  delta_u = 2,
  u_phi = 0.2,
  tau_exp = 0.1,
  normalize = TRUE,
  seed = NULL
)
}
\arguments{
\item{n}{Number of units.}

\item{true_att}{True average treatment effect on the treated.}

\item{beta0, beta1, beta2}{Outcome-model intercept and covariate coefficients.}

\item{theta_spatial}{Coefficient of the spatial confounder in the outcome.}

\item{gamma_interference}{Coefficient of neighbourhood exposure in the
outcome (interference strength).}

\item{sigma_eps}{Outcome noise standard deviation.}

\item{delta_u}{Strength of the spatial confounder in the treatment model
(controls the degree of spatial confounding).}

\item{u_phi}{Range parameter of the confounder's exponential covariance.}

\item{tau_exp}{Spatial decay parameter of the exposure kernel.}

\item{normalize}{Logical; row-normalise the exposure kernel (default \code{TRUE}).}

\item{seed}{Optional integer seed.}
}
\value{
A list with \code{Y}, \code{Z}, \code{X} (matrix with columns \code{X1}, \code{X2}), \code{coords},
the exposure \code{E}, the latent confounder \code{U}, and \code{true_att}.
}
\description{
Draws one data set from the data-generating process of the report
(Equation 2.13 and Section 2.3): an unmeasured spatial confounder \eqn{U(s)}
drawn from an exponential Gaussian random field drives both treatment and
outcome, and the outcome further depends on a neighbourhood-exposure term so
that spatial interference is present. The true ATT is \code{true_att}.
}
\details{
The Gaussian random field is generated directly from its covariance matrix
(exponential covariance, i.e. Matern with smoothness 1/2), so the simulator
has no external dependency.
}
\examples{
sim <- simulate_spatial_causal(n = 200, seed = 42)
str(sim)
}
