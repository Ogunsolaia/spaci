% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching-estimators.R
\name{idaps}
\alias{idaps}
\title{Distance-adjusted propensity score with interference (iDAPS)}
\usage{
idaps(
  Y,
  Z,
  X,
  coords,
  tau = 0.1,
  caliper = 0.25,
  pi_grid_step = 0.1,
  normalize = TRUE,
  match_method = c("greedy", "optimal"),
  seed = NULL,
  level = 0.95
)
}
\arguments{
\item{Y}{Numeric outcome vector.}

\item{Z}{Binary treatment vector (0/1).}

\item{X}{Covariate matrix or data frame.}

\item{coords}{Two-column matrix or data frame of spatial coordinates.}

\item{tau}{Positive spatial decay parameter of the exposure kernel (default
\code{0.1}); see \code{\link[=neighbourhood_exposure]{neighbourhood_exposure()}}.}

\item{caliper}{Maximum acceptable matching distance (default \code{0.25}).}

\item{pi_grid_step}{Grid step for the weight search over the simplex.}

\item{normalize}{Logical; row-normalise the exposure kernel (default \code{TRUE}).}

\item{match_method}{Matching algorithm: \code{"greedy"} (default) matches treated
units in random order to their nearest available control (depends on
\code{seed}); \code{"optimal"} solves the 1:1 assignment minimising total distance
(deterministic, needs the \code{clue} package).}

\item{seed}{Optional integer seed for the (randomised) matching order.}

\item{level}{Confidence level for the reported interval.}
}
\value{
An \code{idaps_fit} object; the selected weights are
reported in \code{weights}.
}
\description{
Estimates the ATT by matching on the composite distance of Equation (2.3),
\eqn{\pi_1 D^{PS} + \pi_2 D^{Spatial} + \pi_3 D^{Interference}}, where the
interference component is the absolute difference in neighbourhood exposure.
The weights \eqn{(\pi_1, \pi_2, \pi_3)} sum to one and are chosen over a grid
to minimise a composite balance score (covariate balance, spatial proximity
and exposure balance), rather than tuned by hand. Setting \eqn{\pi_3 = 0}
recovers DAPS and \eqn{\pi_2 = \pi_3 = 0} recovers naive PS.
}
\examples{
sim <- simulate_spatial_causal(n = 150, seed = 1)
idaps(sim$Y, sim$Z, sim$X, sim$coords, tau = 0.1)
}
