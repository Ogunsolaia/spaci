% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{rand_test}
\alias{rand_test}
\title{Conditional randomization test for no direct effect}
\usage{
rand_test(
  Y,
  Z,
  X,
  coords,
  method = c("recoverUplus", "recoverU", "idaps", "daps", "naive_ps"),
  R = 200,
  tau = 0.1,
  caliper = 0.25,
  matern_method = c("mle", "geoR"),
  matern_nu = 0.5,
  seed = NULL
)
}
\arguments{
\item{Y, Z, X, coords}{Data, as passed to the estimators.}

\item{method}{Estimator to bootstrap: one of \code{"recoverUplus"}, \code{"recoverU"},
\code{"idaps"}, \code{"daps"}, \code{"naive_ps"}.}

\item{R}{Number of randomization draws (default \code{200}).}

\item{tau, caliper, matern_method, matern_nu}{Estimator arguments (as relevant).}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
An object of class \code{spaci_randtest} with the observed statistic, the
p-value and the null draws.
}
\description{
Design-based test of the sharp null that the treatment has no direct effect on
any unit. Treatment is redrawn from the estimated propensity score (so the
reference distribution respects the observed treatment mechanism),
neighbourhood exposures are recomputed, and the estimator is recomputed for
each draw. The two-sided p-value is
\eqn{(1 + \#\{|\hat\tau^{(r)}| \ge |\hat\tau|\}) / (1 + R)}.
}
\examples{
sim <- simulate_spatial_causal(n = 120, seed = 1)
rand_test(sim$Y, sim$Z, sim$X, sim$coords, method = "recoverUplus", R = 50)
}
