% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{boot_spatial}
\alias{boot_spatial}
\title{Spatial block bootstrap confidence interval}
\usage{
boot_spatial(
  Y,
  Z,
  X,
  coords,
  method = c("recoverUplus", "recoverU", "idaps", "daps", "naive_ps"),
  block_size = "auto",
  B = 200,
  tau = 0.1,
  caliper = 0.25,
  matern_method = c("mle", "geoR"),
  matern_nu = 0.5,
  level = 0.95,
  seed = NULL
)
}
\arguments{
\item{Y, Z, X, coords}{Data, as passed to the estimators.}

\item{method}{Estimator to bootstrap: one of \code{"recoverUplus"}, \code{"recoverU"},
\code{"idaps"}, \code{"daps"}, \code{"naive_ps"}.}

\item{block_size}{Side length of the square blocks: \code{"auto"} (default,
\verb{domain width x n^(-1/4)}, giving on the order of sqrt(n) blocks) or a
positive number. The block side should exceed the spatial dependence range.}

\item{B}{Number of bootstrap resamples (default \code{200}).}

\item{tau, caliper, matern_method, matern_nu}{Estimator arguments (as relevant).}

\item{level}{Confidence level.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
An object of class \code{spaci_boot} with the bootstrap \code{se}, percentile
\code{ci}, point estimate, and the retained resample estimates.
}
\description{
Resamples square spatial blocks of units with replacement to build a
bootstrap distribution that reflects spatial dependence. For the doubly robust
estimators (\code{recoverU}/\verb{recoverU+}) it block-resamples the estimator's
influence values (a fast, stable linearised bootstrap that avoids re-kriging
resampled coordinates); for the matching estimators it re-runs the whole
pipeline on each resample (recomputing neighbourhood exposures, with a tiny
coordinate jitter to avoid exact-duplicate ties).
}
\examples{
sim <- simulate_spatial_causal(n = 120, seed = 1)
boot_spatial(sim$Y, sim$Z, sim$X, sim$coords, method = "recoverUplus", B = 50)
}
