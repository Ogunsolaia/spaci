% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bias-bound.R
\name{bias_bound}
\alias{bias_bound}
\title{Post-matching bias bound for an iDAPS fit}
\usage{
bias_bound(
  object,
  Y,
  Z,
  X,
  coords,
  tau = 0.1,
  c_overlap = 1.2,
  matern_method = c("mle", "geoR"),
  matern_nu = 0.5
)
}
\arguments{
\item{object}{An \code{idaps_fit} returned by \code{\link[=idaps]{idaps()}} (supplies the matched
pairs and neighbourhood exposure).}

\item{Y, Z, X, coords}{The same outcome, treatment, covariates and coordinates
passed to \code{\link[=idaps]{idaps()}}.}

\item{tau}{Exposure-kernel bandwidth, used only if \code{object} lacks a stored
exposure (default \code{0.1}).}

\item{c_overlap}{Selection inflation constant on the confounding term
(default \code{1.2}). Matching selects units with systematically larger
confounder values, so the marginal variogram slightly understates the
within-pair confounder differences; \code{c_overlap} \eqn{\approx 1.2} restores a
finite-sample-valid bound (set to \code{1} for the population-level bound).}

\item{matern_method, matern_nu}{Passed to the residual variogram fit; see
\code{\link[=recoverU]{recoverU()}}.}
}
\value{
An object of class \code{spaci_bias_bound}: a list with the covariate,
exposure and confounding bound terms (\code{term_X}, \code{term_E}, \code{term_U}), their
\code{total}, the mean matched distance, and the fitted variogram parameters.
}
\description{
Estimates an upper bound on the confounding bias of an \code{\link[=idaps]{idaps()}} matching
estimate, following the variogram bias bound. A matched-pair difference is a
spatial-differencing operation, so the estimator's bias decomposes into three
pieces controlled by the matched sample:
\deqn{|\mathrm{bias}| \le \|\theta_2\|\,\overline{\|\Delta X\|}
  + |\theta_3|\,\overline{|\Delta E|}
  + c\,|\theta_U|\,\overline{\sqrt{2\gamma_U(d)}},}
where \eqn{\Delta X, \Delta E} are within-pair covariate and neighbourhood-
exposure differences, \eqn{\gamma_U} is the (semi)variogram of the unmeasured
spatial confounder evaluated at the matched distances \eqn{d}, and \eqn{c} is
a selection constant (see \code{c_overlap}). The covariate and exposure
coefficients are taken from a working outcome model, and the confounding term
from the fitted spatial variogram of that model's residuals (whose sill
carries \eqn{\theta_U^2\,\mathrm{Var}(U)}). The confounding term shrinks as
the matched distances shrink, so a smaller caliper yields a smaller bound.
}
\examples{
sim <- simulate_spatial_causal(n = 150, seed = 1)
fit <- idaps(sim$Y, sim$Z, sim$X, sim$coords, tau = 0.1, seed = 1)
bias_bound(fit, sim$Y, sim$Z, sim$X, sim$coords)
}
