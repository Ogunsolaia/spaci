% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{spatial_ate}
\alias{spatial_ate}
\title{Estimate the ATT with all available methods}
\usage{
spatial_ate(
  Y,
  Z,
  X,
  coords,
  tau = 0.1,
  caliper = 0.25,
  matern_method = c("mle", "geoR"),
  level = 0.95,
  seed = NULL,
  methods = c("Naive PS", "DAPS", "iDAPS", "recoverU", "recoverU+")
)
}
\arguments{
\item{Y}{Numeric outcome vector.}

\item{Z}{Binary treatment vector (0/1).}

\item{X}{Covariate matrix or data frame.}

\item{coords}{Two-column matrix or data frame of spatial coordinates.}

\item{tau}{Positive spatial decay parameter of the exposure kernel (default
\code{0.1}); see \code{\link[=neighbourhood_exposure]{neighbourhood_exposure()}}.}

\item{caliper}{Maximum acceptable matching distance (default \code{0.25}).}

\item{matern_method}{Matern estimation engine for the recoverU family,
\code{"mle"} (default) or \code{"geoR"}.}

\item{level}{Confidence level for the reported interval.}

\item{seed}{Optional integer seed for the (randomised) matching order.}

\item{methods}{Character vector selecting which estimators to run. Defaults
to all five.}
}
\value{
A data frame with one row per method (\code{Method}, \code{ATT}, \code{SE},
\code{Lower}, \code{Upper}), with the fitted objects attached as the \code{"fits"}
attribute.
}
\description{
Convenience wrapper that runs the naive propensity score, DAPS, iDAPS,
recoverU and recoverU+ estimators on the same data and collects the point
estimates, standard errors and confidence intervals in a single data frame.
}
\examples{
sim <- simulate_spatial_causal(n = 150, seed = 1)
spatial_ate(sim$Y, sim$Z, sim$X, sim$coords, seed = 1)
}
