% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposure.R
\name{neighbourhood_exposure}
\alias{neighbourhood_exposure}
\title{Neighbourhood exposure via a spatial kernel}
\usage{
neighbourhood_exposure(coords, Z, tau = 0.1, normalize = TRUE)
}
\arguments{
\item{coords}{A two-column matrix or data frame of spatial coordinates.}

\item{Z}{Binary treatment vector (0/1), one entry per row of \code{coords}.}

\item{tau}{Positive spatial decay (bandwidth) parameter of the exponential
kernel. Smaller values concentrate exposure on nearer neighbours.}

\item{normalize}{Logical; if \code{TRUE} (default) each row of the kernel is
normalised to sum to one, so that exposure is a weighted average of
neighbours' treatment. If \code{FALSE}, the raw kernel weights are used, matching
the unnormalised definition in Equation (2.4) of the report.}
}
\value{
A list with components
\describe{
\item{E}{Numeric vector of neighbourhood exposures, one per unit.}
\item{G}{The (possibly row-normalised) kernel matrix with zero diagonal.}
}
}
\description{
Constructs the neighbourhood-exposure mapping
\eqn{E_i = \sum_{j \ne i} G_{ij} A_j} that summarises spatial interference,
where \eqn{G_{ij}} is an exponential kernel of the distance between units
\eqn{i} and \eqn{j}, \eqn{G_{ij} = \exp(-d_{ij} / \tau)} with
\eqn{G_{ii} = 0}. This is the exposure used by \code{\link[=idaps]{idaps()}} and the
recoverU family.
}
\examples{
set.seed(1)
coords <- cbind(runif(20), runif(20))
Z <- rbinom(20, 1, 0.5)
ex <- neighbourhood_exposure(coords, Z, tau = 0.1)
head(ex$E)
}
