% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recoverU.R
\name{recoverU}
\alias{recoverU}
\title{recoverU: doubly robust ATT with a recovered spatial confounder}
\usage{
recoverU(
  Y,
  Z,
  X,
  coords,
  tau = 0.1,
  normalize = TRUE,
  matern_method = c("mle", "geoR"),
  matern_nu = 0.5,
  level = 0.95
)
}
\arguments{
\item{Y}{Numeric outcome vector.}

\item{Z}{Binary treatment vector (0/1).}

\item{X}{Covariate matrix or data frame.}

\item{coords}{Two-column matrix or data frame of coordinates.}

\item{tau}{Positive spatial decay parameter for the exposure kernel used in
the initial outcome model (default \code{0.1}).}

\item{normalize}{Logical; row-normalise the exposure kernel (default \code{TRUE}).}

\item{matern_method}{Matern estimation engine: \code{"mle"} (default, no external
dependency) or \code{"geoR"} (reproduces the reference analysis).}

\item{matern_nu}{Fixed Matern smoothness for the \code{"mle"} engine (default
\code{0.5}, an exponential covariance, which is numerically stable); pass \code{NULL}
to estimate the smoothness freely. Ignored by the \code{"geoR"} engine.}

\item{level}{Confidence level for the reported interval.}
}
\value{
An \code{idaps_fit} object.
}
\description{
Doubly robust ATT estimator that adjusts for spatial confounding through a
partially recovered spatial confounder (Pokal et al., 2023), but does not
adjust the propensity-score / control-outcome models for spatial
interference. Provided as a comparator to \code{\link[=recoverUplus]{recoverUplus()}}.
}
\examples{
sim <- simulate_spatial_causal(n = 150, seed = 1)
recoverU(sim$Y, sim$Z, sim$X, sim$coords)
}
